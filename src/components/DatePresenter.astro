---
const { date, transitionName = null } = Astro.props
---

<time datetime={date.toISOString()} transition:name={transitionName}>
  {
    new Date(date).toLocaleDateString('en-CA', {
      month: 'long',
      day: 'numeric',
      year: 'numeric',
      timeZone: 'utc',
    })
  }
</time>
